[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["UP046"]

[tool.pytest.ini_options]
testpaths = ["app", "tests"]
asyncio_mode = "auto"
